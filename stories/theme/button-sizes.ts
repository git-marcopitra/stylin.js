import { space } from './space';

export const buttonSizes = {
  sm: {
    padding: space.M,
  },
  md: {
    padding: `${space.M} ${space.L}`,
  },
  lg: {
    padding: `${space.L} ${space.XXL}`,
  },
};
